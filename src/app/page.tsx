export default function Home() {
  return (
    <main className="flex min-h-screen flex-col items-center justify-center gap-4 p-8 font-sans">
      <h1 className="text-3xl font-semibold tracking-tight">PARITY</h1>
      <p className="max-w-md text-center text-zinc-600 dark:text-zinc-400">
        Day 1 scaffold — PreStocks-only correctness core (STOCKLANA / $10k
        PreStocks bounty). No wallet, no swap, no Tessera tokens.
      </p>
      <p className="text-sm text-zinc-500">
        Analytics UI lands after Day 1 multipliers + price conventions are
        green.
      </p>
    </main>
  );
}
